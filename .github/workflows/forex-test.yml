name: Forex Test (MT5 + RustDesk)

on:
  workflow_dispatch:
    inputs:
      debug_remote:
        description: 'Remote desktop: none, rustdesk'
        required: false
        default: 'none'
        type: choice
        options:
          - none
          - rustdesk
      debug_timeout_minutes:
        description: 'Minutes to keep runner alive for remote access'
        required: false
        default: '30'

jobs:
  test-mt5:
    runs-on: windows-latest
    timeout-minutes: 90

    steps:
      # --- RustDesk Remote Access (first, so you can observe everything) ---
      - name: Download RustDesk
        if: inputs.debug_remote == 'rustdesk'
        run: |
          $ErrorActionPreference = 'Stop'

          $release = Invoke-RestMethod -Uri "https://api.github.com/repos/rustdesk/rustdesk/releases/latest"
          $version = $release.tag_name
          $asset = $release.assets | Where-Object { $_.name -match "^rustdesk-.*-x86_64\.exe$" } | Select-Object -First 1

          if (-not $asset) {
            Write-Host "::error::Could not find RustDesk x86_64 exe in release $version"
            $release.assets | ForEach-Object { Write-Host "  - $($_.name)" }
            exit 1
          }

          Write-Host "Downloading: $($asset.name) ($version)"
          Invoke-WebRequest -Uri $asset.browser_download_url -OutFile "$env:TEMP\rustdesk.exe" -TimeoutSec 120
          Write-Host "Download complete."

      - name: Start RustDesk and show connection info
        if: inputs.debug_remote == 'rustdesk'
        shell: pwsh
        env:
          RUSTDESK_PASSWORD: ${{ secrets.RUSTDESK_PASSWORD }}
        run: |
          $ErrorActionPreference = 'Stop'
          $bootstrapExe = "$env:TEMP\rustdesk.exe"

          # Use secret password if set; otherwise generate random
          $password = $env:RUSTDESK_PASSWORD
          if ([string]::IsNullOrWhiteSpace($password)) {
            $alphabet = 'abcdefghijkmnopqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789'
            $password = -join (1..16 | ForEach-Object {
              $alphabet[(Get-Random -Maximum $alphabet.Length)]
            })
          }

          # Start bootstrap/portable executable, let it extract the real client
          Start-Process -FilePath $bootstrapExe
          Start-Sleep -Seconds 15

          # Find the actual running RustDesk executable (extracted to LOCALAPPDATA)
          $processes = Get-Process rustdesk -ErrorAction SilentlyContinue
          $actualExe = $processes |
            Where-Object {
              $_.Path -and
              $_.Path -like "$env:LOCALAPPDATA\rustdesk\*"
            } |
            Select-Object -ExpandProperty Path -Unique |
            Select-Object -First 1

          if (-not $actualExe) {
            Write-Host "RustDesk processes:"
            $processes | Format-List Id, SessionId, Path, StartTime
            throw "Could not locate the extracted RustDesk executable."
          }

          Write-Host "Active RustDesk executable: $actualExe"

          # Set permanent password
          $passwordOutput = & $actualExe --password $password 2>&1 | Out-String
          $passwordExitCode = $LASTEXITCODE
          Write-Host "Password command exit code: $passwordExitCode"
          if (-not [string]::IsNullOrWhiteSpace($passwordOutput)) {
            Write-Host "Password command output: [$($passwordOutput.Trim())]"
          }
          Start-Sleep -Seconds 5

          # Get ID with retry
          $id = $null
          for ($i = 1; $i -le 12; $i++) {
            Write-Host "Waiting for RustDesk ID... attempt $i/12"
            $raw = cmd.exe /c "`"$actualExe`" --get-id | more" 2>&1 |
              Out-String
            Write-Host "Raw output: [$($raw.Trim())]"

            $match = [regex]::Match($raw, '(?<!\d)\d{6,12}(?!\d)')
            if ($match.Success) {
              $id = $match.Value
              break
            }
            Start-Sleep -Seconds 5
          }

          if (-not $id) {
            Get-Process rustdesk -ErrorAction SilentlyContinue |
              Format-List Id, SessionId, Path, StartTime
            throw "RustDesk did not return a recognizable ID."
          }

          Write-Host ""
          Write-Host "========================================================"
          Write-Host " RUSTDESK CONNECTION INFO"
          Write-Host "========================================================"
          Write-Host " RustDesk ID: $id"
          Write-Host " Password:    $password"
          Write-Host "========================================================"

      # --- Setup environment ---
      - name: Clone collector repo
        run: git clone --depth 1 https://x-access-token:${{ secrets.K_LINE_COLLECTOR_PAT }}@github.com/sheng9571/k-line-collector.git

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: pip install MetaTrader5 pyarrow huggingface_hub pyyaml numpy

      # --- MT5 Install and Test ---
      - name: Download MT5 installer
        run: |
          $url = "https://download.mql5.com/cdn/web/metaquotes.software.corp/mt5/mt5setup.exe"
          $output = "$env:TEMP\mt5setup.exe"
          Write-Host "Downloading MT5 installer..."
          Invoke-WebRequest -Uri $url -OutFile $output -TimeoutSec 120
          Write-Host "Download complete: $output"

      - name: Install MT5 Terminal (silent)
        run: |
          $installer = "$env:TEMP\mt5setup.exe"
          Write-Host "Installing MT5 Terminal in silent mode..."
          Start-Process -FilePath $installer -ArgumentList "/auto" -Wait -NoNewWindow
          Write-Host "MT5 installation complete."

      - name: Wait for MT5 Terminal process
        run: |
          $timeout = 60
          $elapsed = 0
          while ($elapsed -lt $timeout) {
            $proc = Get-Process -Name "terminal64" -ErrorAction SilentlyContinue
            if ($proc) {
              Write-Host "MT5 Terminal is running (PID: $($proc.Id))"
              break
            }
            Start-Sleep -Seconds 5
            $elapsed += 5
            Write-Host "Waiting for MT5 Terminal... ($elapsed/$timeout s)"
          }
          if (-not (Get-Process -Name "terminal64" -ErrorAction SilentlyContinue)) {
            Write-Host "::warning::MT5 Terminal process not detected after ${timeout}s"
          }

      - name: Test MT5 connection
        env:
          MT5_LOGIN: ${{ secrets.MT5_LOGIN }}
          MT5_PASSWORD: ${{ secrets.MT5_PASSWORD }}
          MT5_SERVER: ${{ secrets.MT5_SERVER }}
        run: |
          python -c "
          import MetaTrader5 as mt5
          import os

          login = int(os.environ['MT5_LOGIN'])
          password = os.environ['MT5_PASSWORD']
          server = os.environ['MT5_SERVER']

          print(f'Connecting to {server} with login {login}...')
          result = mt5.initialize(login=login, password=password, server=server)
          if result:
              info = mt5.terminal_info()
              print(f'Connected successfully!')
              print(f'Terminal: {info.name}')
              print(f'Build: {info.build}')
              print(f'Server: {info.connected}')
              mt5.shutdown()
          else:
              code, msg = mt5.last_error()
              print(f'Connection FAILED: code={code}, msg={msg}')
              exit(1)
          "

      # --- Keep alive for remote debugging ---
      - name: Keep alive for remote debugging
        if: always() && inputs.debug_remote == 'rustdesk'
        run: |
          $minutes = [int]"${{ inputs.debug_timeout_minutes }}"
          Write-Host ""
          Write-Host "========================================================"
          Write-Host " ALL STEPS COMPLETE - KEEPING ALIVE FOR REMOTE DEBUG"
          Write-Host " Runner will shut down in $minutes minutes."
          Write-Host " RustDesk is still active. Connect now if needed."
          Write-Host "========================================================"
          Start-Sleep -Seconds ($minutes * 60)
          Write-Host "Debug timeout reached. Shutting down."
